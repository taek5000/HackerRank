SELECT A.NAME
    , COUNT(*) AS COUNT
FROM ANIMAL_INS A
LEFT OUTER JOIN (SELECT NAME, COUNT(*) AS CNT FROM ANIMAL_INS GROUP BY NAME) B
                ON A.NAME = B.NAME
WHERE B.CNT >= 2
    AND A.NAME IS NOT NULL
GROUP BY A.NAME
ORDER BY A.NAME